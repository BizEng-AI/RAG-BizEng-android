package com.example.myapplication

import android.app.Application

// Minimal Application stub used only during JVM unit tests
class TestApplication : Application() {
    // Intentionally empty - provides a non-null Application instance for constructors in tests
}

