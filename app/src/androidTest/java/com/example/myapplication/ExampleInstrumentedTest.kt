package com.example.rag

/**
 * Legacy example test; disabled because the package name assertion is no longer valid
 * for this app module. Keep as a placeholder or update the assertion if you want
 * a context sanity check.
 */
//@RunWith(AndroidJUnit4::class)
class ExampleInstrumentedTest {
    //@Test
    fun useAppContext_disabled() {
        // Context of the app under test.
        // val appContext = InstrumentationRegistry.getInstrumentation().targetContext
        // assertEquals("com.example.myapplication", appContext.packageName)
    }
}